# Core runtime — shared by all backends (vLLM/Qwen, Vertex/Gemini, Gpu).
numpy>=1.24
pyyaml>=6.0
qdrant-client>=1.12   # pipeline uses query_points() (search() removed in 1.12)
pytest>=7.0

# Backend SDKs (imported lazily — only needed for real, non-mock runs).
openai>=1.30.0        # OpenAI-compatible calls: local vLLM + Gpu LLM endpoint
requests>=2.31        # Gpu embed endpoint (dedicated-domain :predict, TEI)
google-auth>=2.29.0   # GCP bearer-token auth + refresh for Gpu endpoints
google-genai>=0.3.0   # Gemini backend (vertex_ai / gpu *_provider=gemini)
