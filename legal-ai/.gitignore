# Secrets
.env

# Eval / pipeline output
results*.json
submission/
outputs/
cache/
*.zip
eval/data/eval_predictions.json
eval/data/eval_results.json
reembed_checkpoint.json

# HF dataset cache (multi-GB raw parquet — never commit)
corpus/data/hf_cache/

# Expanded corpus is a large rebuildable artifact (~780MB > GitHub 100MB limit) —
# Keep all generated corpus JSON local; rebuild through the ingestion/cleaning pipeline.
corpus/data/*.json

# Corpus build byproducts
corpus/data/coverage_*.json
corpus/data/coverage_*.txt

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.eggs/
dist/
build/
